import styles from './styles/homepage'
import {function1, function2} from './mainContent';

function initialComponents() {
  // select the main div
  // create the left div element with contents
  console.log(function1());
  console.log(function2());
}

// document.body.appendChild(initialComponents()); append new div element
initialComponents();
